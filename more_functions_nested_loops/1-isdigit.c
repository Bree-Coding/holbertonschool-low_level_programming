#include "main.h"

/**
 * _isdigit - Return 1 if c is an uppercase
 * @c: a varriable between 48 and 57
 * Return: 0, 1
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{       return (1); }

	else
	{       return (0); }

}
